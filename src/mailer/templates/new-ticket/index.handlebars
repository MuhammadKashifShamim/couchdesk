<!doctype html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Ticket #{{ticket.uid}}</title>
    <style>
        body{margin:0;}
        section{display:block;}
        img{border:0;}
        img{vertical-align:middle;}
        img{max-width:100%;height:auto;box-sizing:border-box;border:0;}
        ::-moz-selection{background:#39f;color:#fff;text-shadow:none;}
        ::selection{background:#39f;color:#fff;text-shadow:none;}
        section{display:block;}
        .uk-grid{display:-ms-flexbox;display:-webkit-flex;display:flex;-ms-flex-wrap:wrap;-webkit-flex-wrap:wrap;flex-wrap:wrap;margin:0;padding:0;list-style:none;}
        .uk-grid:after,.uk-grid:before{content:"";display:block;overflow:hidden;}
        .uk-grid:after{clear:both;}
        .uk-grid>*{-ms-flex:none;-webkit-flex:none;flex:none;margin:0;float:left;}
        .uk-grid>*>:last-child{margin-bottom:0;}
        .uk-grid{margin-left:-25px;}
        .uk-grid>*{padding-left:25px;}
        @media (min-width:1220px){
        .uk-grid{margin-left:-35px;}
        .uk-grid>*{padding-left:35px;}
        }
        [class*=uk-width]{box-sizing:border-box;width:100%;}
        .uk-width-1-1{width:100%;}
        .uk-clearfix:before{content:"";display:table-cell;}
        .uk-clearfix:after{content:"";display:table;clear:both;}
        .uk-float-left{float:left;}
        .uk-float-right{float:right;}
        [class*=uk-float-]{max-width:100%;}
        @media print{
        *,:after,:before{background:0 0!important;color:#000!important;box-shadow:none!important;text-shadow:none!important;}
        img{page-break-inside:avoid;}
        img{max-width:100%!important;}
        }
        *,*:before,*:after{-webkit-box-sizing:border-box;-moz-box-sizing:border-box;box-sizing:border-box;}
        ::-webkit-scrollbar{background:rgba(255,255,255,0.1);position:absolute;width:5px;height:5px;}
        ::-webkit-scrollbar-thumb{background-color:#9d9d9d;}
        ::-webkit-scrollbar-track{box-shadow:inset 0 0 4px rgba(0,0,0,0);}
        div{margin:0;}
        img{display:inline-block;vertical-align:middle;}
        .site-logo{width:140px!important;}
        .top-nav,.top-bar{background:{{settings.colors.header_background}};height:75px;line-height:75px;}
        .top-nav .title-area .logo,.top-bar .title-area .logo{height:75px;}
        .top-bar{border-bottom:1px solid #0c2139;}
        .top-menu{height:75px;line-height:75px;}
        .top-menu .title{color:{{settings.colors.header_primary}};display:inline-block;font-family:"Roboto","Open Sans",sans-serif;font-size:20px;line-height:normal;margin-right:25px;max-width:175px;text-align:right;vertical-align:middle;}

        h1,h2,h3,h4,h5,h6 {
            margin-bottom: 2px;
        }
        hr {
            display: block;
            margin: 0;
            padding: 0;
            width: 100%;
            background: #ccc;
            height: 3px;
            border: none;
        }
        .strong {
            font-weight: 600;
        }
    </style>
</head>
<body>
    <div class="uk-grid top-nav">
      <div class="uk-width-1-1">
        <div class="top-bar" data-topbar="true">
          <div class="title-area uk-float-left">
            <div class="logo">
              <img src="{{settings.logo}}" alt="Logo" class="site-logo">
            </div>
          </div>
          <section class="top-bar-section uk-clearfix">
            <div class="top-menu uk-float-right">
              <span class="title">{{settings.title}}</span>
            </div>
          </section>
        </div>
      </div>
    </div>
    <div>
        <p>
            A ticket has been submitted by {{ticket.owner.fullname}}. The ticket details are listed below.
        </p>
        <hr>
        <h2>{{{ticket.subject}}}</h2>
        <h5><span class="strong">Ticket Status:</span> {{ticket.status}}</h5>
        <h5><span class="strong">Ticket Project:</span> {{ticket.group.name}}</h5>
        <h5><span class="strong">Ticket Submitted by:</span> {{ticket.owner.fullname}}</h5>
        <h5><span class="strong">Submitted Date:</span> {{ticket.date}}</h5>
        <h5><span class="strong">Ticket Type:</span> {{ticket.type.name}}</h5>
        <h5><span class="strong">Ticket Priority:</span> {{ticket.priority.name}}</h5>
        <h5>
            <span class="strong">Tags:</span>
            <ul>
                {{#each ticket.tags}}
                    <li>{{name}}</li>
                {{/each}}
            </ul>
        </h5>
        <br/>
        <h4>Ticket Issue</h4>
        <hr/>
        <p class="issue">{{{ticket.issue}}}</p>
    </div>
</body>
</html>